import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quizreact.app',
  appName: 'quizreact',
  webDir: 'build',
  bundledWebRuntime: false,

};

export default config;
